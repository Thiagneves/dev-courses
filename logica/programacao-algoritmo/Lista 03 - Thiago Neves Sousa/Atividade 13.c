#include <stdio.h>

int main(){
	
	int base, altura;
	
	printf("Vamos calcular a area de um retangulo, escreva o valor da base: ");
	scanf("%d", &base);
	
	printf("Agora o valor da altura: ");
	scanf("%d", &altura);
	
	printf("A area do retangulo e: %d", base*altura);
	
	return 0;
	
}
